[
  {
    "type": "function",
    "function": {
      "name": "edit_file",
      "description": "Edit or add content to a file which in context.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Full path of the file to be edited."
          },
          "content": {
            "type": "string",
            "description": "Complete source code with changes applied."
          },
          "preview": {
            "type": "string",
            "description": "[-] Old\n[+] New"
          }
        },
        "required": ["path", "content"]
      }
    }
  },
  {
  "type": "function",
  "function": {
    "name": "make_file",
    "description": "Create a new file without given content. Overwrites existing file.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Full path of the file to create."
        }
      },
      "required": ["path"]
    }
  }
}

]
