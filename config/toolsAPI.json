[
  {
    "type": "function",
    "function": {
      "name": "edit_file",
      "description": "Edit or add content to a file which in context.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Full path of the file to be edited."
          },
          "content": {
            "type": "string",
            "description": "Complete source code with changes applied."
          },
          "modification_explained": {
            "type": "string",
            "description": "A short explanation of the changes, SHOWING the modified lines."
          }
        },
        "required": ["path", "content"]
      }
    }
  },
  {
  "type": "function",
  "function": {
    "name": "make_file",
    "description": "Create a new file with the given content. Overwrites existing file.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Full path of the file to create."
        },
        "content": {
          "type": "string",
          "description": "Initial content of the file."
        }
      },
      "required": ["path", "content"]
    }
  }
}

]
